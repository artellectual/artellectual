dependencies:
  build:
    - yarn
    - nodejs
    - npm
  runtime:
    - nginx
    - bash
    - curl
    - s6
    - artellectual-openrc
build:
  commands: |
    yarn install
    yarn run build
  destination: 'dist'
