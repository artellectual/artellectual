dependencies:
  build:
    - yarn
    - nodejs
    - npm
  runtime:
    - nginx
    - bash
    - curl
    - s6
    - artellectual-openrc
build:
  destination: 'dist'
  command: |
    yarn install
    yarn run build
