dependencies:
  build:
    - yarn
    - nodejs
    - npm
  runtime:
    - nginx
    - bash
    - curl
    - s6
    - artellectual-openrc
build:
  destination: 'dist'
  commands: |
    yarn install
    yarn run build
